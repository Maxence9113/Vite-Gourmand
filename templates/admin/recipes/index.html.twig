{% extends 'admin/base_admin.html.twig' %}

{% block title %}Gestion des Recettes - Admin{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/pages/admin-lists.css') }}">
{% endblock %}

{% block content %}
<div class="list-header">
    <h1>
        🍽️ Gestion des recettes
    </h1>
    <a href="{{ path('app_admin_recipes_new') }}" class="btn-add">
        <span data-icon="plus" data-icon-width="16" data-icon-height="16"></span>
        Ajouter une recette
    </a>
</div>

{# Afficher les messages flash #}
{% for message in app.flashes('success') %}
    <div class="flash-success-list">
        {{ message }}
    </div>
{% endfor %}

{% for message in app.flashes('error') %}
    <div class="flash-error-list">
        {{ message }}
    </div>
{% endfor %}

<!-- Liste des recettes -->
<div class="list-container">
    <div class="list-search-bar">
        <div class="list-search-wrapper">
            <span data-icon="search" data-icon-width="20" data-icon-height="20" class="list-search-icon"></span>
            <input type="text" id="searchInput" placeholder="Rechercher une recette..." class="list-search-input">
        </div>
    </div>

    {% if recipes is empty %}
        <div class="list-empty-state">
            <span data-icon="inbox" data-icon-width="48" data-icon-height="48" class="list-empty-icon"></span>
            <p class="list-empty-text">Aucune recette trouvée.</p>
        </div>
    {% else %}
        <div class="list-table-wrapper">
            <table class="list-table medium-list">
                <colgroup>
                    <col>
                    <col>
                    <col>
                    <col>
                </colgroup>
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Titre</th>
                        <th>Description</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    {% for recipe in recipes %}
                    <tr class="recipe-row">
                        <td><strong>{{ recipe.id }}</strong></td>
                        <td style="white-space: nowrap; overflow: hidden; text-overflow: ellipsis;" title="{{ recipe.title }}">{{ recipe.title }}</td>
                        <td style="white-space: nowrap; overflow: hidden; text-overflow: ellipsis;" title="{{ recipe.description }}">{{ recipe.description }}</td>
                        <td>
                            <div class="crud-actions">
                                <a href="{{ path('app_admin_recipes_edit', {id: recipe.id}) }}" class="btn-crud btn-crud-edit">
                                    <span data-icon="edit" data-icon-width="16" data-icon-height="16"></span>
                                    <span class="btn-crud-text">Modifier</span>
                                </a>
                                <form method="post" action="{{ path('app_admin_recipes_delete', {id: recipe.id}) }}" class="delete-form">
                                    <button type="submit" class="btn-crud btn-crud-delete">
                                        <span data-icon="trash-2" data-icon-width="16" data-icon-height="16"></span>
                                        <span class="btn-crud-text">Supprimer</span>
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    {% endif %}
</div>

{# Script pour la recherche en temps réel et le tri #}
<script src="{{ asset('table-sort.js') }}"></script>
<script>
    // Initialiser le tri du tableau (exclure la dernière colonne "Actions")
    initTableSort('recipesTable', {
        excludeColumns: [3]
    });

    // Recherche en temps réel
    document.getElementById('searchInput')?.addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const rows = document.querySelectorAll('.recipe-row');

        rows.forEach(row => {
            const text = row.textContent.toLowerCase();
            row.classList.toggle('hidden', !text.includes(searchTerm));
            if (text.includes(searchTerm)) {
                row.style.display = '';
            } else {
                row.style.display = 'none';
            }
        });
    });

    // Confirmation de suppression
    document.querySelectorAll('.delete-form').forEach(form => {
        form.addEventListener('submit', function(e) {
            const recipeTitle = this.closest('tr').querySelectorAll('td')[1].textContent.trim();
            if (!confirm(`Êtes-vous sûr de vouloir supprimer la recette « ${recipeTitle} » ?`)) {
                e.preventDefault();
            }
        });
    });
</script>
{% endblock %}
