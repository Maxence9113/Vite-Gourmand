{% extends 'base.html.twig' %}

{% block title %}{{ isEdit ? 'Modifier' : 'Ajouter' }} un régime alimentaire{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <h1>{{ isEdit ? 'Modifier le régime alimentaire' : 'Ajouter un nouveau régime alimentaire' }}</h1>
                <a href="{{ path('app_admin_dietetaries') }}" class="btn btn-secondary">
                    Retour à la liste
                </a>
            </div>

            {# Afficher les messages flash #}
            {% for message in app.flashes('success') %}
                <div class="alert alert-success alert-dismissible fade show" role="alert">
                    {{ message }}
                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                </div>
            {% endfor %}

            <div class="card">
                <div class="card-body">
                    {{ form_start(form) }}

                    <div class="mb-3">
                        {{ form_row(form.name) }}
                    </div>

                    {% if isEdit and dietetary.menus|length > 0 %}
                        <div class="alert alert-info">
                            <strong>Information :</strong> Ce régime alimentaire est utilisé par {{ dietetary.menus|length }} menu(s).
                        </div>
                    {% endif %}

                    <div class="d-flex justify-content-between mt-4">
                        <a href="{{ path('app_admin_dietetaries') }}" class="btn btn-secondary">
                            Annuler
                        </a>
                        <button type="submit" class="btn btn-primary">
                            {{ isEdit ? 'Modifier' : 'Créer' }} le régime alimentaire
                        </button>
                    </div>

                    {{ form_end(form) }}
                </div>
            </div>

            {% if isEdit %}
            <div class="mt-3">
                <small class="text-muted">
                    ID du régime alimentaire : {{ dietetary.id }}
                </small>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}
