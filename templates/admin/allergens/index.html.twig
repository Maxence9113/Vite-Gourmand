{% extends 'base.html.twig' %}

{% block title %}Gestion des Allergènes{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1>Gestion des Allergènes</h1>
        <div>
            <a href="{{ path('app_admin_dashboard') }}" class="btn btn-secondary">Retour au Dashboard</a>
            <a href="{{ path('app_admin_allergens_new') }}" class="btn btn-success">Ajouter un allergène</a>
        </div>
    </div>

    {# Afficher les messages flash #}
    {% for message in app.flashes('success') %}
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {% for message in app.flashes('error') %}
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
            {{ message }}
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
    {% endfor %}

    {# Barre de recherche #}
    <div class="row mb-3">
        <div class="col-md-6">
            <input type="text" class="form-control" placeholder="Rechercher un allergène..." id="searchInput">
        </div>
    </div>

    {# Table des allergènes #}
    <div class="card">
        <div class="card-body">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>ID</th>
                        <th>Nom</th>
                        <th>Nombre de recettes</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody id="allergensTable">
                    {% for allergen in allergens %}
                    <tr class="allergen-row">
                        <td>{{ allergen.id }}</td>
                        <td class="fw-bold">{{ allergen.name }}</td>
                        <td>
                            <span class="badge bg-warning">{{ allergen.recipes|length }} recette(s)</span>
                        </td>
                        <td class="text-end">
                            <div class="btn-group" role="group">
                                <a href="{{ path('app_admin_allergens_edit', {id: allergen.id}) }}"
                                   class="btn btn-sm btn-warning">
                                    Modifier
                                </a>
                                <form method="post"
                                      action="{{ path('app_admin_allergens_delete', {id: allergen.id}) }}"
                                      style="display: inline;"
                                      onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer l\'allergène « {{ allergen.name }} » ?')">
                                    <button type="submit" class="btn btn-sm btn-danger">
                                        Supprimer
                                    </button>
                                </form>
                            </div>
                        </td>
                    </tr>
                    {% else %}
                    <tr>
                        <td colspan="4" class="text-center">
                            <div class="alert alert-info mb-0">
                                Aucun allergène trouvé. <a href="{{ path('app_admin_allergens_new') }}" class="alert-link">Créer un nouvel allergène</a>
                            </div>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    {# Informations sur le nombre d'allergènes #}
    <div class="mt-3">
        <p class="text-muted">
            Total : <strong>{{ allergens|length }}</strong> allergène(s)
        </p>
    </div>
</div>

{# Script pour la recherche en temps réel #}
<script>
    document.getElementById('searchInput')?.addEventListener('input', function(e) {
        const searchTerm = e.target.value.toLowerCase();
        const rows = document.querySelectorAll('.allergen-row');

        rows.forEach(row => {
            const text = row.textContent.toLowerCase();
            row.style.display = text.includes(searchTerm) ? '' : 'none';
        });
    });
</script>
{% endblock %}
