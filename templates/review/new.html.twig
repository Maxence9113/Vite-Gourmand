{% extends 'base.html.twig' %}

{% block title %}Laisser un avis{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('styles/pages/auth.css') }}">
    <link rel="stylesheet" href="{{ asset('styles/pages/review.css') }}">
{% endblock %}

{% block body %}
{# ========================================
   NEW REVIEW PAGE - Figma Design
   ======================================== #}
<section class="auth-section">
    <div class="review-new-container">

        {# Card principale #}
        <div class="auth-card">

            {# En-tête #}
            <div class="review-new-header">
                <div class="auth-icon-circle">
                    <span data-icon="edit-3" data-icon-width="28" data-icon-height="28" class="text-white"></span>
                </div>
                <h1 class="auth-title">
                    Partagez votre expérience
                </h1>
                <p class="review-new-header-subtitle">
                    Votre avis nous aide à nous améliorer et aide les autres clients
                </p>
            </div>

            {# Formulaire #}
            {{ form_start(form) }}

                {# Note - Rating #}
                <div class="review-rating-field">
                    {{ form_label(form.rating, 'Votre note', {
                        'label_attr': {
                            'class': 'review-rating-label'
                        }
                    }) }}

                    <div class="review-rating-options">
                        {% for choice in form.rating.vars.choices %}
                            <div>
                                <input type="radio"
                                       id="rating_{{ choice.value }}"
                                       name="{{ form.rating.vars.full_name }}"
                                       value="{{ choice.value }}"
                                       class="review-rating-input">
                                <label for="rating_{{ choice.value }}"
                                       class="review-rating-option-label">
                                    <div class="review-rating-stars">
                                        {% for i in 1..5 %}
                                            <span data-icon="star" data-icon-width="20" data-icon-height="20"
                                                  class="rating-star-{{ choice.value }} {% if i <= choice.value %}review-item-star-icon{% else %}review-item-star-icon-empty{% endif %}"></span>
                                        {% endfor %}
                                    </div>
                                    <span class="review-rating-option-text">{{ choice.label }}</span>
                                </label>
                            </div>
                        {% endfor %}
                    </div>
                    {{ form_errors(form.rating) }}
                </div>

                {# Commentaire #}
                <div class="review-comment-field">
                    {{ form_label(form.comment, 'Votre commentaire (optionnel)', {
                        'label_attr': {
                            'class': 'review-comment-label'
                        }
                    }) }}
                    {{ form_widget(form.comment, {
                        'attr': {
                            'class': 'review-comment-textarea',
                            'placeholder': 'Partagez votre expérience avec nous...'
                        }
                    }) }}
                    {{ form_errors(form.comment) }}
                </div>

                {# Information modération #}
                <div class="review-moderation-notice">
                    <span data-icon="info" data-icon-width="20" data-icon-height="20" class="review-moderation-notice-icon"></span>
                    <div class="review-moderation-notice-content">
                        <strong class="review-moderation-notice-title">Modération des avis</strong>
                        <span>Votre avis sera vérifié par notre équipe avant d'être publié. Cela nous permet de garantir l'authenticité et la qualité des témoignages.</span>
                    </div>
                </div>

                {# Boutons d'action #}
                <div class="review-form-actions">
                    <a href="{{ path('app_order_show', {id: order.id}) }}"
                       class="review-btn-cancel">
                        <span data-icon="x" data-icon-width="18" data-icon-height="18"></span>
                        Annuler
                    </a>
                    <button type="submit"
                            class="review-btn-submit">
                        <span data-icon="send" data-icon-width="18" data-icon-height="18"></span>
                        Envoyer mon avis
                    </button>
                </div>

            {{ form_end(form) }}

        </div>

    </div>
</section>

<script>
// Gestion de la sélection des étoiles
document.addEventListener('DOMContentLoaded', function() {
    const radioInputs = document.querySelectorAll('input[type="radio"][name="{{ form.rating.vars.full_name }}"]');
    const labels = document.querySelectorAll('label[for^="rating_"]');

    // Style initial
    labels.forEach(label => {
        label.addEventListener('mouseenter', function() {
            this.style.borderColor = 'var(--gold)';
            this.style.backgroundColor = 'rgba(212, 175, 55, 0.05)';
        });

        label.addEventListener('mouseleave', function() {
            const input = document.getElementById(this.getAttribute('for'));
            if (!input.checked) {
                this.style.borderColor = 'var(--beige)';
                this.style.backgroundColor = 'white';
            }
        });
    });

    // Gestion de la sélection
    radioInputs.forEach(input => {
        input.addEventListener('change', function() {
            labels.forEach(label => {
                const labelInput = document.getElementById(label.getAttribute('for'));
                if (labelInput.checked) {
                    label.style.borderColor = 'var(--bordeaux)';
                    label.style.backgroundColor = 'rgba(212, 24, 61, 0.05)';
                    label.style.fontWeight = '600';
                } else {
                    label.style.borderColor = 'var(--beige)';
                    label.style.backgroundColor = 'white';
                    label.style.fontWeight = '400';
                }
            });
        });
    });
});
</script>
{% endblock %}