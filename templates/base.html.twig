<!DOCTYPE html>
<html class="fonts-loading">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" type="image/png" href="{{ asset('images/Favicon.png') }}">

        {# Preload des polices Google Fonts critiques #}
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Open+Sans:wght@300;400;500;600;700&display=swap">
        <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Open+Sans:wght@300;400;500;600;700&display=swap" media="print" onload="this.media='all'; document.documentElement.classList.remove('fonts-loading'); document.documentElement.classList.add('fonts-loaded');">
        <noscript>
            <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700;800&family=Open+Sans:wght@300;400;500;600;700&display=swap">
        </noscript>

        {# CSS Critique inline pour éviter le FOUC #}
        <style>
            {{ source('styles/critical.css')|raw }}
        </style>

        {# Preload du CSS principal (non-critique) #}
        <link rel="preload" as="style" href="{{ asset('styles/app.css') }}">

        {% block stylesheets %}
        {% endblock %}

    </head>
    <body>
        {% include "_partials/_navbar.html.twig" %}

        <main>
            {% block body %}{% endblock %}
        </main>

        {% include "_partials/_footer.html.twig" %}

        {% block javascripts %}
            {{ importmap('app') }}
        {% endblock %}

    </body>
</html>
